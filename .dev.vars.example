# Copy to .dev.vars for local development (never commit .dev.vars).
ELEVENLABS_API_KEY=your_key_here

# Optional: Workers AI Gateway id if you route model calls through a gateway.
# CLOUDFLARE_AI_GATEWAY=
